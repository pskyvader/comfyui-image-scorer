# comfyui-image-scorer
A custom scorer for ComfyUI. Configuration files have been consolidated into the `config/` folder. See `config/config.json` for repository-wide pointers (prepare/training configs). Use `deploy.py` to export the ComfyUI node.
